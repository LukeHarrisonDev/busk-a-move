import { Text, View, ScrollView } from 'react-native';

function SettingsScreen() {
    return (
        <ScrollView>
            <Text>
                SettingsScreen
            </Text>
        </ScrollView>
    );
}

export default SettingsScreen;
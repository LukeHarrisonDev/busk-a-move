import { Text, View } from 'react-native';

function LoginAccessScreen(props) {
    return (
        <View>
            <Text>
                LoginAccessScreen
            </Text>
        </View>
    );
}

export default LoginAccessScreen;
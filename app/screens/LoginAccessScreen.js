import { Text, View, ScrollView } from 'react-native';

function LoginAccessScreen() {
    return (
        <ScrollView>
            <Text>
                LoginAccessScreen
            </Text>
        </ScrollView>
    );
}

export default LoginAccessScreen;
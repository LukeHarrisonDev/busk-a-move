import { Text, View, ScrollView } from 'react-native';

function ChatScreen() {
    return (
        <ScrollView>
            <Text>
                ChatScreen
            </Text>
        </ScrollView>
    );
}

export default ChatScreen;
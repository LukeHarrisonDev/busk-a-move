import { Text, View, ScrollView } from 'react-native';

function SignupScreen() {
    return (
        <ScrollView>
            <Text>
                SignupScreen
            </Text>
        </ScrollView>
    );
}

export default SignupScreen;
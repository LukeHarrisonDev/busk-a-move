import { Text, View, ScrollView } from 'react-native';

function ChatsListScreen() {
    return (
        <ScrollView>
            <Text>
                ChatsListScreen
            </Text>
        </ScrollView>
    );
}

export default ChatsListScreen;
import { Text, View, ScrollView } from 'react-native';

function BusksScreen() {
    return (
        <ScrollView>
            <Text>
                BusksScreen
            </Text>
        </ScrollView>
    );
}

export default BusksScreen;